version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./app.db:/app/app.db
    environment:
      - DATABASE_URL=sqlite:///./app.db
    restart: unless-stopped

